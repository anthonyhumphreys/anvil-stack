# Anvil Cell Instructions

Use Anvil Runtime capabilities through ctx. Do not import provider SDKs directly.

This example is intended to stay deployable through the AWS preview adapter.
Avoid services, workflows, and outbound fetch until AWS preview supports them.
