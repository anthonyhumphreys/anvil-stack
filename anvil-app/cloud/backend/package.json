{
  "name": "anvil-backend-spike",
  "version": "0.1.0",
  "private": true,
  "description": "BACKEND-01 spike: Cloudflare Worker + SQLite AccountCoordinator proving sync push/pull, hibernating WebSockets, and eviction recovery.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "conformance": "node conformance/suite.mjs",
    "conformance:fixture": "node conformance/suite.mjs --fixture",
    "verify:hosted-config": "node scripts/verify-hosted-config.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.7",
    "@cloudflare/workers-types": "^5.20260911.1",
    "typescript": "^5.9.2",
    "vitest": "4.1.0",
    "wrangler": "^4.131.0"
  }
}
